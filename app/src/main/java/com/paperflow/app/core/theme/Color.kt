package com.paperflow.app.core.theme

import androidx.compose.ui.graphics.Color

// ─── Primary Brand ───────────────────────────────────────────────────────────
val Amber = Color(0xFFFFD27F)           // Primary accent — soft amber/gold
val AmberDark = Color(0xFFE5B96B)       // Pressed/darker amber
val AmberLight = Color(0xFFFFF3D6)      // Tinted amber surface

// ─── Backgrounds ─────────────────────────────────────────────────────────────
val Cream = Color(0xFFFCF8F2)           // Soft card background
val White = Color(0xFFFFFFFF)           // Main app background
val WhiteElevated = Color(0xFFFEFEFE)   // Slightly elevated surface

// ─── Text ────────────────────────────────────────────────────────────────────
val NearBlack = Color(0xFF1A1A1A)       // Primary text
val Gray = Color(0xFF6B6B6B)            // Secondary text / captions
val GrayLight = Color(0xFF9E9E9E)       // Disabled / placeholder
val Border = Color(0xFFE8E3DB)          // Dividers / borders

// ─── Semantic ────────────────────────────────────────────────────────────────
val Success = Color(0xFF4CAF50)
val SuccessLight = Color(0xFFE8F5E9)
val Warning = Color(0xFFFF9800)
val WarningLight = Color(0xFFFFF3E0)
val Error = Color(0xFFF44336)
val ErrorLight = Color(0xFFFFEBEE)
val Info = Color(0xFF2196F3)

// ─── Document Type Badges ─────────────────────────────────────────────────────
val PdfBadge = Color(0xFFFFCDD2)        // Light Red for PDF
val NoteBadge = Color(0xFFC8E6C9)       // Light Green for NOTE
val ImgBadge = Color(0xFFBBDEFB)
val XlsxBadge = Color(0xFFC8E6C9)
val PptxBadge = Color(0xFFFFE0B2)
val OcrBadge = Color(0xFFE1BEE7)

// ─── Dark Mode ────────────────────────────────────────────────────────────────
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceElevated = Color(0xFF2C2C2C)
val DarkBorder = Color(0xFF3A3A3A)
val DarkTextPrimary = Color(0xFFEEEEEE)
val DarkTextSecondary = Color(0xFFAAAAAA)

// ─── AMOLED Mode ─────────────────────────────────────────────────────────────
val AmoledBackground = Color(0xFF000000)
val AmoledSurface = Color(0xFF0D0D0D)

